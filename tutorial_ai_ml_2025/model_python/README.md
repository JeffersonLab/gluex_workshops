Tutorial using python for AI/ML (companion of TMVA approach in other tutorial)

- model_train.ipynb - notebook for training the model, perform hyperparameter optimization, and additional info
- launch_optuna_dash.py - simple script to launch the Optuna dashboard
- uses `uv` package manager, I think you can still install packages using pyprojec.toml in a new conda environment