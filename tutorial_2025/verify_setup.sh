# A simple script to make sure the user has all the commands for the tutorial available
# to them. 

# TODO: Session presenters should fill in the commands they want to check for below 
# For example, session 2e will need AmpTools libraries available so I'll want some way 
# to check that "root -l -q'loadAmpTools.C'" works